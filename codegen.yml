schema:
  - "schema.graphql"
documents:
  - "countriesQuery.graphql"
overwrite: true
generates:
  ./types/api.ts:
    config:
      immutableTypes: true
      noNamespaces: true
    plugins:
      - "typescript-common"
      - "typescript-client"
